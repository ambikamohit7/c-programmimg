#include<stdio.h>
#include<math.h>
int main() {
    int a,b,p;
    printf("enter a");
    scanf("%d", &a);

    printf("enter b");
    scanf("%d", &b);

    p=2(a+b);
    printf("the perimeter is :-%d", p);
    return 0;
}