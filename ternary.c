// this program is for ternary operator.
#include <stdio.h>
int main()
{
    system("cls");

    int age = 16;
    printf("You are %s \n ", (age >= 18) ? "Adult" : "Not Adult");
    // in avove 8th line we have use the ternary operator.
    return 0;
}